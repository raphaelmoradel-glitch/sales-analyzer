# Sales Analyzer (Python)

Application Python permettant d’analyser des données de ventes depuis des fichiers CSV.
Le projet inclut une interface graphique (Tkinter) avec filtres, statistiques et graphiques.

## Prérequis
- Python 3.10+ recommandé

## Installation
```bash
pip install -r requirements.txt
